{
  "name": "my-frontend-project",
  "version": "0.0.1",
  "description": "Ontario.ca Frontend Project (TODO: Replace with a description of your project)",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && eleventy",
    "debug": "rm -rf dist && DEBUG=* eleventy",
    "serve": "eleventy --serve",
    "test": "mocha './src/tests/**/*.spec.js' && npx nightwatch"
  },
  "author": "TODO changeme",
  "license": "Apache 2.0",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "@ongov/ontario-frontend": "latest",
    "chromedriver": "^125.0.3",
    "mocha": "^10.4.0",
    "nightwatch": "^3.6.3"
  },
  "dependencies": {
    "chai": "^4.4.1"
  }
}
